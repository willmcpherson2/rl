{
  "name": "rl",
  "version": "0.1",
  "scripts": {
    "build:server": "npx webpack --config server.config.js",
    "build:client": "npx webpack --config client.config.js",
    "build:static": "cp -r static/* dist/client",
    "build": "npm run build:server && npm run build:client && npm run build:static"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "@types/ws": "^8.5.10",
    "ts-loader": "^9.5.1",
    "typescript": "^4.9.5",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "ws": "^8.16.0"
  }
}
